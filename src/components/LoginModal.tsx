import React from 'react'

const LoginModal = ({
    isOpen,
    setIsOpen
}:{
    isOpen: boolean
    setIsOpen: (isOpen: boolean) => void
}) => {
  return (
    <div>
      
    </div>
  )
}

export default LoginModal
